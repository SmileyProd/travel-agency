{% extends '/back/base_back.html.twig' %}

{% block title %}Editer - Catégorie{% endblock %}

{% block main %}
    <h1>Editer la catégorie n°{{circuit_category.id}}</h1>

    {{ include('/back/category/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a role="button" class="btn btn-info" href="{{ path('admin_category_index') }}">Retour à la liste</a>

    {{ include('/back/category/_delete_form.html.twig') }}
{% endblock %} {# main #}
