{% extends '/back/base_back.html.twig' %}

{% block title %}Voir - Circuit{% endblock %}

{% block main %}
    <h1>Catégorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th scope="row">Nom</th>
                <td>{{ circuit_category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a role="button" class="btn btn-info" href="{{ path('admin_category_index') }}">Retour à la liste</a>

    <a role="button" class="btn btn-secondary" href="{{ path('admin_category_edit', {'id': circuit_category.id}) }}">Editer</a>

    {{ include('back/category/_delete_form.html.twig') }}
{% endblock %}{# main #}
